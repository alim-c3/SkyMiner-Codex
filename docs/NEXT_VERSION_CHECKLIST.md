# Next Version Checklist (v0.2)

- [ ] Pipeline passes the full `LightCurve` into reporting so plots correspond to the actual candidate
- [ ] Persist decimated light curve samples per candidate (size-bounded)
- [ ] Add `uv` install instructions and lockfile guidance
- [ ] Add CI workflow running offline tests
- [ ] Add a `--no-validate` and `--no-report` CLI flags (explicit)
- [ ] Add batch ingestion caching + provenance (data source, query params, download timestamps)

